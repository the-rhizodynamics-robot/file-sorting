profiles {
    local {
        process {
            executor = 'local'
            container = 'ghcr.io/the-rhizodynamics-robot/file-sorting-env:latest'
        }
         docker {
            enabled = true         
        }
    }
}